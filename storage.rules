rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // รูปยา — ทุกคนอ่านได้, เฉพาะ authenticated user เขียนได้
    match /drug-images/{filename} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
